export default defineAppConfig({
  ui: {
    primary: 'blue',
    gray: 'cool',
    modal: {
      overlay: {
        background: 'bg-[rgba(0,0,70,0.30)]'
      },
      width: 'max-w-full min-w-[90%]',
    }
  }
}) 